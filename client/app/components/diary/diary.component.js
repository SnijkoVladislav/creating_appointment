import template from './diary.html';
import controller from './diary.controller';
import './diary.scss';

let diaryComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default diaryComponent;
